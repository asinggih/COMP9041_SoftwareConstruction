#!/bin/bash


# Write a program digits.sh that reads from standard input and writes to standard output
# mapping all digit characters whose values are less than 5 into the character '<' and 
# all digit characters whose values are greater than 5 into the character '>'. 
# The digit character '5' should be left unchanged.



while read line
do
	echo "$line"
done

"
